/* eslint-disable */

// import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.one.bank.MyBank',
  appName: 'bank_one',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
// module.exports = config;
